## Summary

<!-- What does this PR change and why? -->

## Test plan

- [ ] `npm run build`
- [ ] `npm run tauri dev` / `npm run tauri build` (if Rust or Tauri config changed)
- [ ] Manual recording test on Windows (if capture/audio/encoder changed)
